(() => { async function init() { if (!auth.requireDriver()) return false; const u = auth.getUser() || {}; document.getElementById("driverShell").innerHTML = `<header class="border-b bg-white"><div class="mx-auto flex h-16 max-w-6xl items-center px-5"><a href="./driver-dashboard.html" class="font-bold">BusBooking <span class="text-cyan-700">Driver</span></a><div class="ml-auto flex items-center gap-4"><span class="text-sm font-semibold">${admin.esc(admin.pick(u,"fullName","FullName") || `${admin.pick(u,"firstName","FirstName")} ${admin.pick(u,"lastName","LastName")}`)}</span><button id="driverLogout" class="rounded-lg border px-3 py-2 text-sm">Logout</button></div></div></header>`; document.getElementById("driverLogout").onclick = auth.logout; try { const me = await auth.driverMe(); auth.updateStoredUser(me); } catch {} return true; } window.driverLayout = { init }; })();
