(async()=>{if(!await driverLayout.init())return;try{const me=await auth.driverMe(),now=new Date(),year=now.getFullYear(),month=now.getMonth()+1;document.getElementById("profileRoot").innerHTML=`<section class="rounded-2xl border bg-white p-5 shadow-sm"><h2 class="text-xl font-bold">${admin.esc(admin.pick(me,"fullName","FullName"))}</h2><p class="mt-2 text-sm text-slate-600">Phone: ${admin.esc(admin.pick(me,"phone","Phone"))} · License: ${admin.esc(admin.pick(me,"licenseNumber","LicenseNumber"))} · Status: ${admin.esc(admin.pick(me,"status","Status"))} · Hired: ${admin.esc(admin.fmt(admin.pick(me,"hireDate","HireDate")))}</p></section>`;const[today,tomorrow,after,monthly,yearly]=await Promise.all([admin.request("/api/driver-hashboard/today-trip-driver"),admin.request("/api/driver-hashboard/tomorrow-trip-driver"),admin.request("/api/driver-hashboard/after-tomorrow"),admin.request(`/api/driver-hashboard/monthly/${year}/${month}`),admin.request(`/api/driver-hashboard/yearly/${year}`)]);admin.setLoading(false);document.getElementById("statsRoot").innerHTML=`<div class="grid gap-4 sm:grid-cols-2"><div class="rounded-2xl border bg-white p-5"><p class="text-sm text-slate-500">Trips this month</p><p class="text-3xl font-bold">${admin.pick(monthly,"count","Count")||0}</p></div><div class="rounded-2xl border bg-white p-5"><p class="text-sm text-slate-500">Trips this year</p><p class="text-3xl font-bold">${admin.pick(yearly,"count","Count")||0}</p></div></div>`;const root=document.getElementById("tripsRoot"),cols=[{label:"Trip ID",keys:["tripId","TripId"]},{label:"Departure",keys:["departureTime","DepartureTime"],format:admin.fmt},{label:"Arrival",keys:["arrivalTime","ArrivalTime"],format:admin.fmt},{label:"Route",keys:["startCity","StartCity"],format:(v,o)=>`${v} → ${admin.pick(o,"endCity","EndCity")}`},{label:"Bus",keys:["busNumber","BusNumber"]}];[["Today",today],["Tomorrow",tomorrow],["After tomorrow",after]].forEach(([name,data],i)=>{root.insertAdjacentHTML("beforeend",`<section><h2 class="mb-3 text-xl font-bold">${name}</h2><div id="driverTrips${i}"></div></section>`);admin.table(`driverTrips${i}`,data,cols)})}catch(e){admin.setLoading(false);admin.alert(e.message)}})();
